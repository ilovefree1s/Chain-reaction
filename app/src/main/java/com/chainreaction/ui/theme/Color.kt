package com.chainreaction.ui.theme

import androidx.compose.ui.graphics.Color

// The one colour outside the neon black/orange/blue palette: destructive actions
// (delete a course, end a round) stay red so they never read as just another accent.
// Everything else lives in ui/Neon.kt.
val Attack = Color(0xFFFF5A4D)
